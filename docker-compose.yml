services:
  ignite-call-next-14: 
    container_name: ignite-call-next-14
    image: bitnami/postgresql
    ports:
     - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=docker
      - POSTGRESQL_PASSWORD=docker
      - POSTGRESQL_DATABASE=ignitecallnext14